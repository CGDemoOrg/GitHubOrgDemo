# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
#     - uses: actions/checkout@v2
#     - name: Set up JDK 1.8
#       uses: actions/setup-java@v1
#       with:
#         java-version: 1.8
    - uses: actions/checkout@v2
    - name: Build with Maven
      run: mvn -B install --file pom.xml
    - name: Configure AWS credentials from Test account
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

    - name: Copy files to the test website with the AWS CLI
      run: |
        aws s3 sync */DemoProject-0.0.1-SNAPSHOT.war s3://github.action.demo
#     - name: SonarQube Scan
#       uses: monstar-lab-oss/sonarqube-github-action@v1.0.1
#       with:
#         url: ${{ secrets.SONAR_HOST }}
#         token: ${{ secrets.SONAR_TOKEN }}
#     - name: Copy single file to remote
#       uses: garygrossgarten/github-action-scp@release
#       with:
#           local: "*/DemoProject-0.0.1-SNAPSHOT.war"
#           remote: "/home/ec2-user/remote_deploy/"
#           host: '15.207.42.252'
# #           host : '192.168.0.6'
#           username: 'ec2-user'
#           #password: ${{ secrets.PASSWORD }}
#           privateKey: ${{ secrets.SSH_KEY }}
#           passphrase: ${{ secrets.SSH_PASS_PHRASE }}
        




#     - name : Copying files to server
#       uses: appleboy/scp-action@master
#       with:
#           host: '15.207.42.252'
#           username: 'ec2-user'
#           key: ${{ secrets.SSH_KEY }}
#           passphrase: ${{ secrets.SSH_PASS_PHRASE }}
#           rm: true
# #           source: "/home/runner/work/DemoProject/DemoProject/target/DemoProject-0.0.1-SNAPSHOT.war"

#           source: "DemoProject-0.0.1-SNAPSHOT.war"
#           target: "/home/ec2-user/remote_deploy"
    
